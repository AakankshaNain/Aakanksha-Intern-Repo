**When should you use Redux instead of useState?**
1. When the same state has to be shared between multiple components, Redux can be used instead of creating the state separately in each component using useState.
2. useState is component-specific, whereas Redux can be used globally.